apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: analytics-ingress
spec:
  rules:
  - host: ""
    http:
      paths:
      - pathType: Prefix
        path: "/"
        backend:
          service:
            name: analytics-service
            port:
              number: 80
